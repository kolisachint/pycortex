# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Environments
.venv/
venv/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.pyright/

# uv
# (uv.lock is committed)

# TUI parity harness (bun deps + the staged copy of the TS source it reads).
# The goldens themselves ARE committed, so tests need neither bun nor hoocode.
packages/tui/testkit/reference/node_modules/
packages/tui/testkit/reference/.hoocode-src/

# Local agent-harness state (scheduled tasks carry this machine's timestamps).
.agents/

# OS
.DS_Store
