[project]
name = "cortexcode-ai-util"
version = "0.0.3"
description = "Cortex AI leaf: util"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "cortexcode-ai-types>=0.0.3,<0.1.0",
]

[tool.cortex]
publish = false

[tool.hatch.build.targets.wheel]
packages = [
    "src/cortex",
]

[tool.uv.sources.cortexcode-ai-types]
workspace = true

[build-system]
requires = [
    "hatchling",
]
build-backend = "hatchling.build"
