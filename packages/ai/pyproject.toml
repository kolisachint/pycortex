[project]
name = "cortexcode-ai"
version = "0.0.1"
description = "Provider-agnostic LLM streaming API for cortex"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.cortex]
publish = false

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cortex"]
