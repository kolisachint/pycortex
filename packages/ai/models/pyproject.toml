[project]
name = "cortexcode-ai-models"
version = "0.0.2"
description = "Cortex AI leaf: models"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "cortexcode-ai-types>=0.0.2,<0.1.0",
]

[tool.cortex]
publish = false

[tool.hatch.build.targets.wheel]
packages = [
    "src/cortex",
]

[tool.uv.sources.cortexcode-ai-types]
workspace = true

[build-system]
requires = [
    "hatchling",
]
build-backend = "hatchling.build"
