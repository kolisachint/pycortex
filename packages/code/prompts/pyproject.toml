[project]
name = "cortexcode-cli-prompts"
version = "0.0.2"
description = "Cortex CLI leaf: prompts"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.cortex]
publish = false

[tool.hatch.build.targets.wheel]
packages = [
    "src/cortex",
]

[build-system]
requires = [
    "hatchling",
]
build-backend = "hatchling.build"
