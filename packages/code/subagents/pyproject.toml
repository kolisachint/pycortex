[project]
name = "cortexcode-cli-subagents"
version = "0.0.3"
description = "Cortex CLI leaf: subagents"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.cortex]
publish = false

[tool.hatch.build.targets.wheel]
packages = [
    "src/cortex",
]

[build-system]
requires = [
    "hatchling",
]
build-backend = "hatchling.build"
