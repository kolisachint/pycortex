[project]
name = "cortexcode-cli-extensions"
version = "0.0.3"
description = "Cortex CLI leaf: extension/plugin system"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.cortex]
publish = true

[tool.hatch.build.targets.wheel]
packages = [
    "src/cortex",
]

[build-system]
requires = [
    "hatchling",
]
build-backend = "hatchling.build"
