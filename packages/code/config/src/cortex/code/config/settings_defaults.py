"""Default settings values."""

from cortex.code.config.settings_types import (
    BranchSummarySettings,
    CompactionSettings,
    ContextGcSettings,
    ImageSettings,
    MarkdownSettings,
    ProviderRetrySettings,
    RetrySettings,
    Settings,
    TerminalSettings,
    ToolOutputSettings,
    VoiceSettings,
    WarningSettings,
    WebtoolsSettings,
)

DEFAULT_SETTINGS = Settings(
    transport="auto",
    steering_mode="one-at-a-time",
    follow_up_mode="one-at-a-time",
    compaction=CompactionSettings(
        enabled=True,
        reserve_tokens=16384,
        keep_recent_tokens=20000,
        max_context_ratio=0.75,
    ),
    tool_output=ToolOutputSettings(
        max_bytes=32 * 1024,
        max_lines=800,
    ),
    context_gc=ContextGcSettings(enabled=True),
    voice=VoiceSettings(silence_ms=800),
    webtools=WebtoolsSettings(timeout_secs=15),
    disabled_tools=[],
    tool_output_display="standard",
    branch_summary=BranchSummarySettings(
        reserve_tokens=16384,
        skip_prompt=False,
    ),
    retry=RetrySettings(
        enabled=True,
        max_retries=3,
        base_delay_ms=2000,
        provider=ProviderRetrySettings(max_retry_delay_ms=60000),
    ),
    hide_thinking_block=False,
    quiet_startup=False,
    collapse_changelog=False,
    enable_install_telemetry=True,
    enable_skill_commands=True,
    enable_subagent=True,
    warm_subagents=False,
    max_subagent_depth=2,
    nested_subagent_concurrency=2,
    enable_todo_write=True,
    enable_plugin_tools=False,
    defer_mcp_schemas=True,
    enable_web_tools=False,
    enable_browser_tools=False,
    enable_browser_live_preview=False,
    enable_file_tools=False,
    enable_embsearch_tools=True,
    embsearch_threshold_bytes=0,
    light=False,
    terminal=TerminalSettings(
        show_images=True,
        image_width_cells=60,
        clear_on_shrink=False,
        show_terminal_progress=False,
        chime_on_turn_complete=False,
    ),
    images=ImageSettings(
        auto_resize=True,
        block_images=False,
    ),
    double_escape_action="tree",
    tree_filter_mode="default",
    editor_padding_x=0,
    autocomplete_max_visible=5,
    markdown=MarkdownSettings(code_block_indent="  "),
    warnings=WarningSettings(anthropic_extra_usage=True),
    packages=[],
    extensions=[],
    skills=[],
    prompts=[],
    slash_commands=[],
    themes=[],
)
